    var config = {
        type: 'line',
        data: {
            labels: [@foreach ($dataset as $key)'{{$key->tanggal}}',@endforeach],
            datasets: [{
                        label: 'Data Asli',
                        backgroundColor: window.chartColors.red,
                        borderColor: window.chartColors.red,
                        data: [@foreach ($dataset as $key){{(float)($key->sembuh)}}, @endforeach],
                        fill: false,
                       },
                       {
                        label: 'Model Chen',
                        fill: false,
                        backgroundColor: window.chartColors.blue,
                        borderColor: window.chartColors.blue,
                        data: [
                            @foreach ($chen_prediction as $key)
                                @if (is_null($key['prediction']))
                                null,
                                @else
                                {{(float)($key['prediction'])}},
                                @endif
                            @endforeach
					    ],
				       },
            ]
        },
        options: {
            responsive: true,
            title: {
                display: true,
                text: 'Perbandingan Hasil Prediksi'
            },
            plugins: {
                datalabels: {
                display: false,
                },
            },
            tooltips: {
                mode: 'index',
                intersect: false,
            },
            hover: {
                mode: 'nearest',
                intersect: true
            },
            scales: {
                xAxes: [{
                    display: true,
                    scaleLabel: {
                        display: true,
                        labelString: 'Tanggal'
                    }
                }],
                yAxes: [{
                    display: true,
                    scaleLabel: {
                        display: true,
                        labelString: 'Sembuh'
                    }
                }]
            }
        }
    }
